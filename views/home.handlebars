      <div class="row">
        <div class="my-container">
          <div class="col-md-12 my-column">
            <div class="row">
            {{#each category}}
            <div class="col-lg-4 col-md-6 col-sm-6 col-xs-12 blogCategory-box blogcategory">
               <div class="imgcontainer"><div class="item">
                <a data-key="{{this.key}}" href="/listblog/{{this.key}}" class="blogctid"><img alt="{{this.name}}" src="{{this.icon}}"  /></a>
                </div></div>
                <h2>
                  {{this.name}}
                </h2>
                <p>
                  {{this.info}}                
                  <a data-key="{{this.key}}" href="/listblog/{{this.key}}" class="blogctid">Learn More »</a>
                </p>
              </div>
            {{/each}}
          </div>
        </div>
      </div>
    </div>
